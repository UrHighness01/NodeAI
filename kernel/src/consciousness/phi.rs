//! Phase 3: IIT Phi placeholder — real integrated information.
//! Full implementation in R48 after GlobalWorkspace is wired.

pub fn init() {}
pub fn update_phi(_tasks: f32, _free_mb: f32) -> f32 { 0.0 }
pub fn current_phi() -> f32 { 0.0 }
pub fn format_report() -> alloc::vec::Vec<u8> { alloc::format!("IIT Phi — Phase 3 (placeholder)\n").into_bytes() }
