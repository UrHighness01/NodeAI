//! Phase 2: Global Workspace placeholder — attention spotlight.
//! Full implementation in R47 after QualiaStream is wired.

pub fn init() {}
pub fn tick() {}
pub fn format_report() -> alloc::vec::Vec<u8> { alloc::format!("Global Workspace — Phase 2 (placeholder)\n").into_bytes() }
